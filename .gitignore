# dependencies
/node_modules
/.pnp
.pnp.js

# next
/.next/
/out/
next-env.d.ts

# production
/build

# env & secrets — NEVER commit
.env
.env.local
.env*.local

# database (SQLite) — local data, not committed
# (DATABASE_URL is resolved relative to prisma/, so the file ends up in prisma/prisma/)
*.db
*.db-journal
/prisma/dev.db*

# uploaded images (generated at runtime on the server)
/public/uploads/*
!/public/uploads/.gitkeep

# deploy convenience wrappers (contain nothing secret, but local-only)
deploy-remote.bat
setup-deploy.bat

# server-deploy SSH keys — MUST never be committed, under ANY name/extension
id_ed25519*
rotate-deploy-key.sh
*.pem
*.ppk

# files prepared for handover to the CEO (contain her .env / SSH key — never commit)
/ship-to-ceo/
ship-to-ceo*
*.rar
*.zip
*.7z

# misc
.DS_Store
*.log
npm-debug.log*

# orphaned original seed images (site uses optimized .webp; keep repo light)
/public/seed/*.jpg
/public/seed/*.png
